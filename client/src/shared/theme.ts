const body = "arboria, sans-serif";
const header = "arboria, sans-serif";
const accent = "gemeli-mono, serif";

export const themeFonts = { body, header, accent };

const yellow = "#FFDF8E";
const darkBlue = "#131A43";
const offWhite = "#FFFCF5";

export const themeColors = { yellow, darkBlue, offWhite };

// default : mobile
// iPhone 11 - 4 cols
const xs = `424px`;

// iPad - 8 cols
const sm = `768px`;

// iPad Pro
const md = `1024px`;

// iMac - 12 cols
const lg = `1280px`;

// macbook pro
const xlg = `1440px`;

export const themeBreakpoints = {
  xs,
  sm,
  md,
  lg,
  xlg,
};
